{
  "name": "no-molestar",
  "version": "2.0.0",
  "description": "Retro-futuristic Spotify web player",
  "type": "module",
  "scripts": {
    "dev": "vite src/renderer",
    "build": "vite build src/renderer",
    "preview": "vite preview --outDir src/renderer/dist"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
